<!DOCTYPE html>
<html lang="en">
  <%- include('partials/head.ejs') %>
  <body>
    <h1>Index Page</h1>
    <p style="color: <%=balanceColor %>">Remaining Balance: <%=balance %></p>
    <table>
        <% for (let i = 0; i < budgetItems.length; i++) { %>
            <tr>
                <td><%=budgetItems[i].date %></td>
                <td><a href="/budgets/<%=budgetItems[i]._id %>"
                    ><%=budgetItems[i].name %></a</td>
                <td><%=budgetItems[i].from %></td>
                <td><%=budgetItems[i].amount %></td>
            </tr>
            <% } %>
    </table>
    <a href="/budgets/new"><button>Add New Item</button></a>
  </body>
</html>
